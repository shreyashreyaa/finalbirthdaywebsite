[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  # You can set VITE_SITE_PASSWORD in Netlify UI under Site settings → Build & deploy → Environment
  # VITE_SITE_PASSWORD = "your_secret_password"
